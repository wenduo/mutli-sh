#!/bin/sh
source ./hosts
for i in $hosts
do
	#auto ssh -t wangwenduo@$i sudo "sudo yum install expect"
	#auto ssh -t wangwenduo@$i sudo "export PATH=$PATH:/home/wangwenduo/bin/"
	#auto ssh -t wangwenduo@$i sudo "export PATH=$PATH:/home/wangwenduo/bin/"
done
